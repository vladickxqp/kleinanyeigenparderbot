# Frontend build/runtime config.
# In dev, Vite proxies /api to http://localhost:8000 (see vite.config.ts), so no
# API URL is needed. In production the nginx container proxies /api to the api
# service, so again no absolute URL is required. Add VITE_* vars here if needed.
